import React, { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
import { useStore } from '../../../common/Context';

function TokenVerificationP(){
    console.log(useParams());
    
    useEffect(()=>{
    },[]);

    return (
        <></>
    )
}
export default TokenVerificationP;